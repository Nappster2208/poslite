import { Inter, Lusitana, Nunito } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });

export const lusitana = Lusitana({
  weight: ["400", "700"],
  subsets: ["latin"],
});

export const nuntio = Nunito({ subsets: ["latin"] });
